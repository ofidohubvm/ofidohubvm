[build]
  functions = "functions"
  command = "npm run build"
  publish = "build"

[context.production]
  command = "npm run build"
  publish = "build"

[context.deploy-preview]
  command = "npm run build"
  publish = "build"

[context.branch-deploy]
  command = "npm run build"
  publish = "build"

[context.dev]
  command = "npm run build"
  publish = "build"